defmodule QuizServerTest do
  @moduledoc """
  Tests for QuizServer
  """
  use ExUnit.Case
  doctest QuizServer

  test "is testing working?" do
    assert true == true
    refute true == false
  end
end
